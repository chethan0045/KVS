{
  "name": "kvs-bricks",
  "version": "1.0.0",
  "description": "KVS Bricks Management System",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npx ng build --configuration production",
    "start": "cd backend && npm start"
  }
}
